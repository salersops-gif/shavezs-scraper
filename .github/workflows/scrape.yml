# This workflow is RETIRED — superseded by scraper.yml (the 3-step pipeline).
# Renamed trigger so it no longer fires on scrape-job dispatches.
name: LeadEngine — Scrape Job (RETIRED)

on:
  workflow_dispatch:  # manual only, never auto-triggered
    inputs:
      reason:
        description: 'This workflow is retired. Use scraper.yml instead.'
        required: false

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. scraper.yml handles all scrape-job dispatches."
